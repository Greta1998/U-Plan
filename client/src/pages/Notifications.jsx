export default function Notifications() {
  return (
    <div>
      <h1 className="page-heading">Notifications</h1>
      <p className="page-sub">Backend notification endpoints are not wired yet — this page is a placeholder matching the prototype.</p>
      <div className="card" style={{ padding: "24px", marginTop: "20px" }}>
        <p style={{ fontSize: "14px", color: "#5a6080" }}>
          When you add a push or email service, you can list alerts here using the same layout as the HTML prototype.
        </p>
      </div>
    </div>
  );
}
